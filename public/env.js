// Default runtime env used by `npm run dev` and `vite preview`.
// In production the Docker entrypoint regenerates this file from container
// environment variables (see env.sh), so VITE_* values can change per
// deployment without rebuilding the image.
window.__RUNTIME_CONFIG__ = {};
